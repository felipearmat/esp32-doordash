name: "Intercom Audio Gateway"
version: "0.1.1"
slug: "intercom_audio_gateway"
description: "PCM audio router with echo cancellation (speexdsp) for the ESP32 modules and the Home Assistant browser client"
arch: [amd64, aarch64, armv7, i386]
startup: services
boot: auto
host_network: true
init: false
map:
  - config:ro
options: {}
schema: {}
